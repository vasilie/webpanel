# RedDot zone

Admin panel for RedDot.zone app


## Available Scripts

Before everything run:
`yarn install`

#### Development

`yarn start`

#### Production

Build project with:

`yarn build`

You can serve it with:

`yarn global add serve`

`serve -s build`






